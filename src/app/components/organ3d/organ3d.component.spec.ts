import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Organ3dComponent } from './organ3d.component';

describe('Organ3dComponent', () => {
  // let component: Organ3dComponent;
  // let fixture: ComponentFixture<Organ3dComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ Organ3dComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(Organ3dComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
